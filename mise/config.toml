[tools]
go = "1.22"
python = "3.12"
node = "20"

[settings]
# Automatically install missing tools on mise install
auto_install = true
# Keep downloaded archives to avoid re-downloading
always_keep_download = true
