- defaults:
          link:
                  relink: true

- clean: ["~"]

- create:
          - ~/Workspace
          - ~/.config

- link:
          ~/.gitconfig: gitconfig
          ~/.config/nvim: nvim

- shell:
          - [~/.dotfiles/scripts/run-setup.sh, Installing requirements]
          - [git submodule update --init --recursive, Installing submodules]
          - [
                    "nvim +PlugUpdate +PlugClean! +qa +UpdateRemotePlugins",
                    Installing and updating plugins,
            ]
